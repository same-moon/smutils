pid = os.fork()
if pid == 0: # child
    \esys.stdout.write( "I am child pid %d\n" % ( os.getpid() ) )
    os._exit( 0 )
else:        # parent
    sys.stdout.write( "I am parent of child pid %d\n" % ( pid ))
